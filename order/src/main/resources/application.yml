spring:
  application:
    name: order
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://112.74.59.190:3316/miaosha?useUnicode=true&characterEncoding=utf8&allowMultiQueries=true&useLegacyDatetimeCode=false&serverTimezone=Asia/Shanghai
  redis:
    host: 112.74.59.190
    port: 6379
    timeout: 3
    pool-max-total: 1000
    pool-max-wait: 500
    pool-max-idle: 500
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
  resources:
    static-locations: classpath:/static/

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/


mybatis:
  type-aliases-package: self.lcw.order.entity,self.lcw.order.dto
  configuration:
    map-underscore-to-camel-case: true
    default-fetch-size: 100
    default-statement-timeout: 3000
  mapper-locations: classpath:/order/dao/*.xml

server:
  servlet:
    context-path: /order