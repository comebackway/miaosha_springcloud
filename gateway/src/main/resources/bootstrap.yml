spring:
  application:
    name: gateway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: dev
zuul:
  routes:
#    aaaa:
#      path: /haha/**
#      serviceId: user

     USER: /haha/**

  ignored-patterns:

    - /haha/user/goods/to_list
    - /user/user/goods/to_list



